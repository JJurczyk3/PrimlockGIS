# Primelock GIS 高级 GIS 课程作业源代码报告

## 引言

### AI 使用与学术诚信说明

本项目所有原始源代码注释及项目文件最初均以英文编写；本打印稿中的中文说明、文件说明以及代码注释译文由大型语言模型（LLM）翻译生成。

根据本人的 AI 使用诚信声明：从数据读取、基础数据模型、几何运算、插值、规则格网、TIN、等值线生成直至等值线追踪（含等值线追踪）的全部实现，以及课程作业要求的所有核心内容，均由本人手工编写。LLM 编码代理主要用于把项目整理为稳定、可下载并可离线运行的 Windows 应用程序；协助完成用户界面和交互功能；审查并清理代码注释；以及在最终提交前把说明和注释翻译为中文。

本声明旨在清楚区分本人独立完成的课程核心 GIS 工作与后期使用 LLM 所完成的工程化、界面、发布、审查和翻译工作。最终电子版源代码 ZIP 保留规范的生产源代码；本中文打印稿用于实体提交和人工审阅。

### 项目简介

Primelock GIS 是一个面向高级 GIS 课程作业的终端交互式 GIS 应用程序。程序直接实现课程所需的核心 GIS 算法，不依赖外部 GIS 算法库。它能够读取采样点数据，建立规则格网和不规则三角网（TIN），生成与追踪等值线，进行第一阶段拓扑处理，并在终端中以 ANSI 颜色、Unicode 字符和 Braille 点阵进行交互式绘制。

本报告首先说明项目结构、方法和假设，随后只列出课程评审最重要的 GIS 源代码。每个代码文件均带有中文章节标题、完整仓库路径和用途说明。Python 标识符、公开 API 名称及命令词保持英文，以保证电子源代码与可执行程序的一致性；原始英文注释和文档字符串在打印稿中提供中文译文。协议标记、异常文本和运行时字符串仍保留其源码原文；中文版应用在用户界面边界提供对应的中文标题、控件、状态和说明。

## 项目标识

- 应用程序名称：**Primelock GIS**
- Windows 可执行文件：`PrimelockGIS.exe`
- Python 导入包：`primelock_gis`
- Python 发行元数据名称：`primlockgis`（为兼容现有构建流程而保留）
- 发布版本：以 `pyproject.toml` 中的版本为准
- 语言版本：英语（默认）与简体中文（`zh-CN`）

## 数据处理流程

```text
CSV 采样点
    -> 字段结构验证与数值读取
    -> x/y 原点归一化
    -> ProjectConfig 项目配置
    -> 规则插值格网与 TIN
    -> 格网或 TIN 等值线
    -> 可选的第一阶段拓扑处理
    -> 与后端无关的场景和视口
    -> 终端渲染器、查看器与控制面板
```

`ProjectConfig` 是项目构建的输入。`ProjectState` 将通过验证的采样点及其派生格网和 TIN 组合为一个一致状态，防止查看器误用来自不同数据集的模型。如果替换数据集的读取或模型构建失败，程序会继续保留并显示原项目状态。

## 课程要求追踪表

| 课程功能 | 主要实现文件 |
| --- | --- |
| CSV 数据读取与字段验证 | `core/load_data.py` |
| 点、矢量及拓扑数据模型 | `core/models/vector.py` |
| 反距离权重插值（IDW） | `core/algorithms/interpolation.py` |
| 方向加权插值 | `core/algorithms/interpolation.py` |
| 规则格网生成与细分 | `core/algorithms/grid.py`, `core/models/grid.py` |
| TIN 生成与采样 | `core/algorithms/tin.py`, `core/models/tin.py` |
| 格网与 TIN 等值线 | `core/algorithms/contour.py` |
| 等值线追踪 | `core/algorithms/contour.py`, `core/models/contour.py` |
| 第一阶段拓扑构建 | `core/algorithms/topology.py` |
| 视口与场景转换 | `core/rendering` |
| 终端渲染 | `ui/terminal/canvas.py`, `renderer2d.py` |

表中路径均相对于 `src/primelock_gis`。

## GIS 方法与实现假设

### 坐标和高程

数据读取器要求 CSV 包含编号、点名、x、y 和 z 字段，并拒绝缺失字段或非有限数值。归一化操作从每个点的 x、y 坐标中分别减去最小 x、最小 y，而 z 高程保持不变。这只是局部坐标原点平移：程序不会推断坐标参考系统，不会改变单位，不会执行大地基准转换，也不会修改高程。因此，距离、面积、等值距及高程均沿用输入数据的原始单位。

### 反距离权重插值

目标位置的 IDW 高程按下式计算：

```text
z(target) = sum(w_i * z_i) / sum(w_i), 其中 w_i = 1 / d_i^2
```

如果目标位置与某个采样点完全重合，程序直接返回该点高程，避免除零。当前实现使用所有输入点，权重指数固定为 2，不设置搜索半径。这些选择直接写在 `idw_value` 中，不隐藏在界面逻辑里。

### 方向加权平均

程序把目标点周围的完整圆周划分为等角度扇区，从每个非空扇区选择距离最近的一个采样点，再对所选点执行相同的反距离平方加权。这样可以减弱同一方向上采样点聚集造成的偏差。默认使用四个扇区，即每个象限一个扇区。

### 规则格网

`x_divisions` 和 `y_divisions` 表示格网单元数量，因此节点数组在对应方向上比划分数多一个节点。节点均匀分布在采样点范围内。格网构建要求划分数为正整数，所有输入值有限，并且 x、y 两个方向均具有非零范围。格网细分把每个原单元进一步划分，新节点高程通过双线性插值得到。

### 不规则三角网（TIN）

TIN 采用增量 Bowyer-Watson 方法构建：

1. 建立覆盖全部采样点的人工超级三角形。
2. 逐个插入真实采样点。
3. 删除外接圆包含新插入点的三角形。
4. 使用空洞边界与新点重新三角化。
5. 删除与人工超级三角形顶点相连的三角形。
6. 根据共享边建立三角形邻接关系。

输入坐标和高程必须为有限值；x/y 位置必须唯一；至少需要三个不共线采样点。三角形内部的高程使用重心坐标进行线性插值。

### 等值线

格网等值线算法检查每个格网单元的四条边。当边两端高程跨越目标等值面时，程序在线段上进行线性插值，得到交点。若一个单元出现四个交点，则使用单元中心的平均高程确定连接方式。TIN 等值线对每个三角形应用相同的边交点规则。随后，算法利用共享边键将原始线段追踪成开放或闭合折线。对于恰好等于等值线高程的节点，程序使用 `interval / 5000` 的确定性微小扰动，避免顶点奇异情况。

### 拓扑

第一阶段拓扑处理会删除连续重复点，检测线段交点，分割线划，建立共享节点和弧段，并为符合条件的简单闭合序列建立多边形记录。默认容差为几何模块中的 `EPS`。共线重叠、孔洞、复杂多边形验证以及完整平面拓扑强制不属于本阶段范围。

### 渲染与交互

GIS 算法只负责产生数据模型；场景构建器把模型转换为可绘制对象；视口负责世界坐标与终端坐标之间的转换；终端渲染器再使用 ANSI 颜色、Unicode 线条和 Braille 点阵绘制最终画面。这种分层避免界面代码与 GIS 计算互相混杂。

## 英语与中文版本

英语版和中文版共享同一套 GIS 模型、算法与发布代码。英语是默认语言，中文版通过 `zh-CN` 语言参数启动。语言层只改变用户可见的标题、菜单、按钮、状态、帮助、错误信息和随附文档；它不复制或修改核心 GIS 算法。这样可以避免两个代码分支产生不同计算结果。

## 打印顺序

本报告的源代码正文按以下顺序排列：

1. 数据读取与几何基础。
2. 矢量、格网、TIN 和等值线数据模型。
3. 插值、格网、TIN、等值线与拓扑核心算法。
4. 纸质篇幅不再展开界面、平台适配和打包代码；这些内容保留在电子源代码中。

## 完整源代码与打印稿的关系

打印稿经过严格筛选，只保留课程评审最重要的 GIS 数据读取、数据模型和核心算法。界面布局、终端平台适配和打包逻辑不在纸质正文中展开。版本化源代码 ZIP 包含生产代码、构建脚本、配置、锁文件、许可证说明和随附数据集；纸质打印稿是便于评阅的中文阅读版本，不替代电子源代码。

## 已知限制

- 对于近似重复点或共圆点，Bowyer-Watson 结果受固定数值容差影响。
- TIN 查找缓存假定模型构建后不再直接修改几何数据。
- 第一阶段拓扑不会处理共线重叠或多边形孔洞。
- 大型模型的重建目前仍在前台同步执行。
- 完整鼠标支持取决于终端模拟器提供的控制台事件能力。
